# Netlify build settings. Netlify detects Next.js and installs its Next.js adapter automatically,
# so no plugin entry is needed here. Leave the publish directory at the Next.js default.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
